/**
 * Server module boundary (CHOKEPOINT-PLAN.md §9.2 src/server, §10.1): the
 * local-first application process hosting the §10.3 API, /metrics, and the
 * built static app.
 */

export * from "./server";
