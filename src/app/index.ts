/**
 * Application state module boundary (CHOKEPOINT-PLAN.md §9.2, src/app/).
 *
 * Full app state (startup, share state) arrives in Phase 3. Only the version
 * identifier is defined now so the entry point is buildable.
 */

export const appVersion = "0.1.0-phase0";
