# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
build/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment / secrets (SECURITY.md 14.1: .env files remain ignored)
.env
.env.*
!.env.example

# Editor / OS (Windows)
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# Test / coverage output
coverage/
.vitest/

# Cesium / map cache artifacts
.cesium/

# OS caches
.cache/

# Raw downloaded research sources (kept locally for verification, never committed:
# provider terms of use / size / redistribution concerns)
research/sources/raw/
# EXCEPTION: the archived CBP sample used by tests is US-gov public-domain
# data and the fixture-first suite depends on it (ADR-0013).
!research/sources/raw/cbp-bwt-sample-2026-09-10.json

# Cesium runtime assets (synced from node_modules by scripts/sync-cesium.mjs)
public/cesium/
