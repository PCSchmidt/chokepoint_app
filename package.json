{
  "name": "chokepoint",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Provenance-aware transportation intelligence for global chokepoints (fixture-mode Phase 0/1)",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "debug": "tsx scripts/aisstream-debug.ts",
    "dev": "vite",
    "eval": "tsx scripts/run-evaluation.ts",
    "lint": "tsc --noEmit",
    "perf:replay": "tsx scripts/perf-replay.ts",
    "prebuild": "node scripts/sync-cesium.mjs",
    "predev": "node scripts/sync-cesium.mjs",
    "qa:browser": "tsx scripts/qa-browser.ts",
    "smoke": "tsx scripts/aisstream-smoke.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "happy-dom": "^20.14.3",
    "playwright": "^1.63.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  },
  "license": "MIT",
  "author": "Chris Schmidt (PCSchmidt)",
  "dependencies": {
    "cesium": "^1.145.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
